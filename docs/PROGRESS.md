# Прогресс

## Этап 1 — RX38 Schema Reverse Engineering

- CONFIRMED: найдены 5 RX38, из них 4 уникальны; 46 уникально учитываемых `Tconstr`; длина строки — 200 полей.
- CONFIRMED: parser сохраняет исходные токены, кавычки, decimal comma, newline и кодировку.
- CONFIRMED: `tools/rx38_diff.py` сравнивает выбранные конструкции и имеет режим `group-by-profile`.
- CONFIRMED: безопасный writer не меняет неизвестные позиции и блокирует попытку их записи.
- CONFIRMED: типизированная `Rx38Construction` хранит только доказанные поля и полный `raw_fields`.
- CONFIRMED: `ProfileRepository` нормализует пробелы, регистр, `x/х/×`, decimal comma и возвращает `AMBIGUOUS` без автоматического выбора.
- CONFIRMED: полная 200-позиционная карта и файловая инвентаризация сформированы.
- PROBABLE: 14 полей имеют сильное, но пока не однозначное сопоставление.
- UNKNOWN: 131 поле не интерпретируется.

Независимая нормативная верификация расчётов RX3 не выполнена; `READY_FOR_ISSUE` не заявляется.
